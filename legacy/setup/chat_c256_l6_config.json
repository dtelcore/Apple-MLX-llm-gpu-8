{
  "model": {
    "name": "Chat C=256 L=6 T=256",
    "vocab_size": null,
    "max_len": 256,
    "embedding_dim": 256,
    "num_heads": 8,
    "num_layers": 6,
    "dropout_prob": 0.0,
    "tie_embeddings": true,
    "norm_type": "rmsnorm",
    "pos_encoding": "rope",
    "init_scale": 0.02,
    "residual_scale": true,
    "layer_strategy": "resident",
    "description": "Chat fine-tune recipe. C=256 L=6 H=8 T=256, GPT-2 residual scale, 4000 BPE merges. Dataset is data/chat_train.txt only (not combined data/)."
  },
  "dataset": {
    "name": "chat_train",
    "combine": false,
    "tokenizer": "bpe",
    "bpe_merges": 4000,
    "vocab_size": null
  },
  "weight_initialization": {},
  "hyperparameters": {
    "name": "Chat C=256 L=6 T=256",
    "learning_rate": 0.00025,
    "weight_decay": 0.01,
    "batch_size": 4,
    "num_epochs": 1,
    "warmup_steps": 400,
    "gradient_clip": 1.0,
    "gradient_accumulation_steps": 4,
    "window_stride": 64,
    "min_lr_ratio": 0.1,
    "optimizer": "adamw",
    "beta1": 0.9,
    "beta2": 0.999,
    "epsilon": 1e-8
  },
  "metadata": {
    "created": "chat-c256-l6-t256",
    "description": "From-scratch chat run on data/chat_train.txt. Do not --menu. Do not resume run-new-8by8 (312 vocab) or run8+16 tokens.npy."
  }
}
