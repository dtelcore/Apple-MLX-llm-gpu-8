{
  "model": {
    "name": "Chat C=512 L=6 T=512",
    "vocab_size": null,
    "max_len": 512,
    "embedding_dim": 512,
    "num_heads": 8,
    "num_layers": 6,
    "dropout_prob": 0.0,
    "tie_embeddings": true,
    "norm_type": "rmsnorm",
    "pos_encoding": "rope",
    "init_scale": 0.02,
    "residual_scale": true,
    "layer_strategy": "stream",
    "description": "Chat C=512 L=6 T=512. H=8, residual_scale=on, layers=stream. Trains only data/fact_overfit_train.jsonl. 2 GB process cap is hardcoded."
  },
  "dataset": {
    "name": "fact_overfit",
    "path": "data/fact_overfit_train.jsonl",
    "combine": false,
    "tokenizer": "bpe",
    "bpe_merges": 4000,
    "vocab_size": null
  },
  "weight_initialization": {},
  "hyperparameters": {
    "name": "Chat C=512 L=6 T=512",
    "learning_rate": 0.0005,
    "weight_decay": 0.01,
    "batch_size": 8,
    "num_epochs": 1,
    "warmup_steps": 50,
    "gradient_clip": 1.0,
    "gradient_accumulation_steps": 2,
    "window_stride": 64,
    "min_lr_ratio": 0.1,
    "optimizer": "adamw",
    "beta1": 0.9,
    "beta2": 0.999,
    "epsilon": 1e-08
  },
  "metadata": {
    "created": "chat-c512-l6-facts",
    "description": "From-scratch overfit of data/fact_overfit_train.jsonl only. combine=false; path wins over data/. Do not --resume chat8b, chat_c256_l12, or the aborted combined-corpus chat_c512_l6 run."
  }
}
