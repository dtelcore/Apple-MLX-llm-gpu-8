{
  "model": {
    "name": "Stories C=256 L=6 T=256",
    "vocab_size": null,
    "max_len": 256,
    "embedding_dim": 256,
    "num_heads": 8,
    "num_layers": 6,
    "dropout_prob": 0.0,
    "tie_embeddings": true,
    "norm_type": "rmsnorm",
    "pos_encoding": "rope",
    "init_scale": 0.02,
    "residual_scale": true,
    "description": "Stable 2 GB story recipe. C=256, L=6, H=8, T=256, GPT-2 residual scale 1/sqrt(2L). ~6M params."
  },
  "dataset": {
    "name": "data_dir",
    "combine": false,
    "tokenizer": "bpe",
    "bpe_merges": 4000,
    "vocab_size": null
  },
  "weight_initialization": {},
  "hyperparameters": {
    "name": "Stories C=256 L=6 T=256",
    "learning_rate": 0.0009,
    "weight_decay": 0.01,
    "batch_size": 4,
    "num_epochs": 1,
    "warmup_steps": 1000,
    "gradient_clip": 1.0,
    "gradient_accumulation_steps": 4,
    "window_stride": 128,
    "min_lr_ratio": 0.1,
    "optimizer": "adamw",
    "beta1": 0.9,
    "beta2": 0.999,
    "epsilon": 1e-8
  },
  "metadata": {
    "created": "story-c256-l6-t256",
    "description": "L=6 + residual_scale for the 8 GB M3 Air. Train 5k–10k steps. Reuse run8+16 tokens.npy only if you copy vocab.json (same BPE)."
  }
}
