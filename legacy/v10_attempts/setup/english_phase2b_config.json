{
  "model": {
    "name": "english_phase2b",
    "vocab_size": null,
    "max_len": 256,
    "embedding_dim": 256,
    "num_heads": 8,
    "num_layers": 6,
    "dropout_prob": 0.0,
    "tie_embeddings": true,
    "norm_type": "rmsnorm",
    "pos_encoding": "rope",
    "init_scale": 0.02,
    "residual_scale": true,
    "layer_strategy": "resident",
    "description": "Phase 2b dense inject snapshot. Architecture frozen by English-Phase1 on --resume. C=256 L=6 H=8 T=256. Same BPE. Do not rebuild vocab. 2 GB cap hardcoded."
  },
  "dataset": {
    "name": "english_phase2b_mix",
    "path": "data/english_phase2b_mix.txt",
    "combine": false,
    "tokenizer": "bpe",
    "bpe_merges": 4000,
    "vocab_size": null
  },
  "weight_initialization": {},
  "hyperparameters": {
    "name": "english_phase2b",
    "learning_rate": 0.00006,
    "weight_decay": 0.01,
    "batch_size": 4,
    "num_epochs": 1,
    "warmup_steps": 100,
    "gradient_clip": 1.0,
    "gradient_accumulation_steps": 4,
    "window_stride": 128,
    "min_lr_ratio": 0.1,
    "optimizer": "adamw",
    "beta1": 0.9,
    "beta2": 0.999,
    "epsilon": 1e-08
  },
  "metadata": {
    "created": "english-phase2b-dense-inject",
    "description": "Targeted geo/organ/prime inject at ~15% fact frames. Varied frames, no 300x. Resume English-Phase1 with --dataset-path data/english_phase2b_mix.txt --reset-lr-schedule. Leave data/english_phase2_mix.txt on disk. Product stays chat_facts_v7. Unguided will not resume this dir."
  }
}
