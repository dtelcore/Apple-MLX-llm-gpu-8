{
  "model": {
    "name": "chat_facts_v10",
    "vocab_size": null,
    "max_len": 384,
    "embedding_dim": 512,
    "num_heads": 8,
    "num_layers": 12,
    "dropout_prob": 0.1,
    "tie_embeddings": true,
    "norm_type": "rmsnorm",
    "pos_encoding": "rope",
    "init_scale": 0.02,
    "residual_scale": true,
    "layer_strategy": "resident",
    "description": "v10 integration mix. C=768 L=16 H=16 T=512 English headroom, not v9 recitation C=512 L=16. dropout_prob is unused (no VJP dropout). Do not combine data/. Do not --resume v7/v8/v9 or English-Phase1. 2 GB cap hardcoded."
  },
  "dataset": {
    "name": "chat_facts_v10",
    "path": "data/chat_facts_v10.jsonl",
    "combine": false,
    "tokenizer": "bpe",
    "bpe_merges": 5000,
    "vocab_size": null
  },
  "weight_initialization": {},
  "hyperparameters": {
    "name": "chat_facts_v10",
    "learning_rate": 0.00006,
    "weight_decay": 0.02,
    "batch_size": 8,
    "num_epochs": 1,
    "warmup_steps": 200,
    "gradient_clip": 1.0,
    "gradient_accumulation_steps": 1,
    "window_stride": 128,
    "min_lr_ratio": 0.1,
    "optimizer": "adamw",
    "beta1": 0.9,
    "beta2": 0.999,
    "epsilon": 1e-08
  },
  "metadata": {
    "created": "chat-facts-v10-integration",
    "description": "From-scratch on data/chat_facts_v10.jsonl (v9 uniques, native User:/Assistant: plus trailing User: stop, core 40x / tail 10x, ~45% facts + wiki). No wrapper frames. Fresh BPE. Do not --resume v7, v8, v9, English-Phase1, or chat_facts_v10_4/5/6. Product chat stays chat_facts_v7. Do not Load this dir until inject + User generate pass."
  }
}
