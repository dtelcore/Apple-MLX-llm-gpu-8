{
  "model": {
    "name": "english_phase1",
    "vocab_size": null,
    "max_len": 256,
    "embedding_dim": 256,
    "num_heads": 8,
    "num_layers": 6,
    "dropout_prob": 0.0,
    "tie_embeddings": true,
    "norm_type": "rmsnorm",
    "pos_encoding": "rope",
    "init_scale": 0.02,
    "residual_scale": true,
    "layer_strategy": "resident",
    "description": "Phase 1 English foundation. C=256 L=6 H=8 T=256, GPT-2 residual scale. Trains only data/train.txt. dropout_prob is unused (no VJP dropout). Do not combine data/. Do not --resume v7/v8/v9. 2 GB cap hardcoded."
  },
  "dataset": {
    "name": "train",
    "path": "data/train.txt",
    "combine": false,
    "tokenizer": "bpe",
    "bpe_merges": 4000,
    "vocab_size": null
  },
  "weight_initialization": {},
  "hyperparameters": {
    "name": "english_phase1",
    "learning_rate": 0.00013,
    "weight_decay": 0.01,
    "batch_size": 4,
    "num_epochs": 1,
    "warmup_steps": 1000,
    "gradient_clip": 1.0,
    "gradient_accumulation_steps": 4,
    "window_stride": 128,
    "min_lr_ratio": 0.1,
    "optimizer": "adamw",
    "beta1": 0.9,
    "beta2": 0.999,
    "epsilon": 1e-08
  },
  "metadata": {
    "created": "english-phase1-prose",
    "description": "From-scratch on data/train.txt only. New BPE. Do not combine data/*.txt. Do not --resume cabinet checkpoints. Product chat stays chat_facts_v7."
  }
}
