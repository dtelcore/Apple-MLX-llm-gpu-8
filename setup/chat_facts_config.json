{
  "model": {
    "name": "Chat C=512 L=6 T=256",
    "vocab_size": null,
    "max_len": 256,
    "embedding_dim": 512,
    "num_heads": 8,
    "num_layers": 6,
    "dropout_prob": 0.1,
    "tie_embeddings": true,
    "norm_type": "rmsnorm",
    "pos_encoding": "rope",
    "init_scale": 0.02,
    "residual_scale": true,
    "layer_strategy": "stream",
    "description": "Phase 1 chat cabinet. C=512 L=6 T=256. Trains only data/chat_facts.jsonl. 2 GB cap is hardcoded."
  },
  "dataset": {
    "name": "chat_facts",
    "path": "data/chat_facts.jsonl",
    "combine": false,
    "tokenizer": "bpe",
    "bpe_merges": 4000,
    "vocab_size": null
  },
  "weight_initialization": {},
  "hyperparameters": {
    "name": "Chat C=512 L=6 T=256",
    "learning_rate": 0.0005,
    "weight_decay": 0.01,
    "batch_size": 8,
    "num_epochs": 1,
    "warmup_steps": 50,
    "gradient_clip": 1.0,
    "gradient_accumulation_steps": 2,
    "window_stride": 64,
    "min_lr_ratio": 0.1,
    "optimizer": "adamw",
    "beta1": 0.9,
    "beta2": 0.999,
    "epsilon": 1e-08
  },
  "metadata": {
    "created": "chat-facts-phase1",
    "description": "From-scratch phase 1 on data/chat_facts.jsonl only. Do not --resume old chat/story checkpoints. Do not combine data/."
  }
}
