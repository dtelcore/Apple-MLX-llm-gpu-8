{
  "model": {
    "name": "Chat C=512 L=16 T=512 v9",
    "vocab_size": null,
    "max_len": 512,
    "embedding_dim": 512,
    "num_heads": 8,
    "num_layers": 16,
    "dropout_prob": 0.1,
    "tie_embeddings": true,
    "norm_type": "rmsnorm",
    "pos_encoding": "rope",
    "init_scale": 0.02,
    "residual_scale": true,
    "layer_strategy": "stream",
    "description": "v9 chat cabinet: colliding families cut/split. Trains only data/chat_facts_v9.jsonl. Keep v7 as product. Do not --resume v7/v8. 2 GB cap hardcoded."
  },
  "dataset": {
    "name": "chat_facts_v9",
    "path": "data/chat_facts_v9.jsonl",
    "combine": false,
    "tokenizer": "bpe",
    "bpe_merges": 4000,
    "vocab_size": null
  },
  "weight_initialization": {},
  "hyperparameters": {
    "name": "Chat C=512 L=16 T=512 v9",
    "learning_rate": 0.00015,
    "weight_decay": 0.01,
    "batch_size": 8,
    "num_epochs": 1,
    "warmup_steps": 50,
    "gradient_clip": 1.0,
    "gradient_accumulation_steps": 3,
    "window_stride": 128,
    "min_lr_ratio": 0.1,
    "optimizer": "adamw",
    "beta1": 0.9,
    "beta2": 0.999,
    "epsilon": 1e-08
  },
  "metadata": {
    "created": "chat-facts-v9-anti-collide",
    "description": "From-scratch on data/chat_facts_v9.jsonl only. No make_fact_mix --linked on same sources. Distinct where/country Assistants. Thin primes/organs; pathogen symptom dup dropped; year rows de-collided. Do not --resume v7, v8, or Unguarded-Initialv8-Run. Product chat stays chat_facts_v7."
  }
}
